# Template — copy to secrets.properties (gitignored) and add your OWN Google Maps Platform key
# to switch map rendering to the Google Maps SDK. Empty here means keyless OSM (the default).
# The public repo/APK ships zero keys (design §6.6).
MAPS_API_KEY=
